m=10
seatbooked =0
sum_income = 0
cost =0
row = int(input('Enter no of rows- \n'))
seat = int(input('Enter no. of seats- \n'))
Totalseat = row*seat
booked_t_Person =[[None for j in range(seat)]for i in range(row)]
class arrangement:
def araangement_maker():
seat_arra = { }
for i in range(row):
seat_row = { }
for j in range(Seat):
seat_row[str(j+1)]= 'S'
seat_arrangement[str(i)]=seat_row
return seat_arrangement

def find_percent():
percent = (seatbooked/Totalseat)*100
return percent

class_call = arrangetable = class_call.arrangement_maker()

while m!=0
print('1 for Show the seat \n2 To Buy a Ticket \n3 For Statistics ',
          '\n4  Show booked Tickets User Info \n0 for Exit')
    m = int(input('Select Option - '))
    if m == 1:
        if seat < 10:
            for seat in range(seat):
                print(seat, end='  ')
            print(seat)
        else:
            for seat in range(10):
                print(seat, end='  ')
            for seat in range(10, seat):
                print(seat, end=' ')
            print(seat)
              if seat < 10:
            for num in tablearrange.keys():
                print(int(num)+1, end='  ')
                for no in tablearrange[num].values():
                    print(no, end='  ')
                print()
        else:
            count = 0
            for num in tablearrange():
                if int(list(tablearrange.keys())[count]) < 9:
                    print(int(num)+1, end='  ')
                else:
                    print(int(num)+1, end=' ')
                countk = 0
                for no in tablearrange[num].values():
                    if int(list(tablearrange[num].keys())[countk]) <= 10:
                        print(no, end='  ')
                    else:
                        print(no, end='  ')
                        print()
